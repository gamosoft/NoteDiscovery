services:
  notediscovery:
    image: ghcr.io/gamosoft/notediscovery:latest
    container_name: notediscovery
    ports:
      - "8000:8000"
    volumes:
      # Persist notes and data
      - ./data:/app/data
      # Custom plugins
      - ./plugins:/app/plugins
      # Custom themes (optional - defaults from image used if not mounted)
      - ./themes:/app/themes
      # Custom locales/translations (optional - defaults from image used if not mounted)
      - ./locales:/app/locales      
      # Config file (created automatically if missing)
      - ./config.yaml:/app/config.yaml
    restart: unless-stopped
    environment:
      - TZ=UTC
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://localhost:8000/health')"]
      interval: 60s
      timeout: 3s
      retries: 3
      start_period: 5s

